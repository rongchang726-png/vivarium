# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Saved worlds (downloaded by the app)
vivarium-tick*.json

# Node (none used, but just in case)
node_modules/
npm-debug.log*

# Downloaded tooling — not part of the repo
mcp-publisher.exe
mcp-publisher*.tar.gz

# Game session / wallet state (player progress, not code)
game/.session.json
game/.wallet.json
game/.server-state.json
game/_*
